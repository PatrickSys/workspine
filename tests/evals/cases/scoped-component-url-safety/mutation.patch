diff --git a/lib/index.js b/lib/index.js
--- a/lib/index.js
+++ b/lib/index.js
@@ -87,3 +87,3 @@ function validate (name) {
-      if (encodeURIComponent(user) === user && encodeURIComponent(pkg) === pkg) {
+      if (encodeURIComponent(user) === user || encodeURIComponent(pkg) === pkg) {
         return done(warnings, errors)
       }
